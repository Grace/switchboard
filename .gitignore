.env
.env.*
!.env.example
.local/
data/
config.json
*.pem
*.key
*.tfstate*
*.tfvars
.terraform/
**/__pycache__/
.pytest_cache/
.venv/
bin/
*.zip
*.bundle
coverage.out
/gateway

# Local dev stack: Ed25519 signing seed and bearer tokens
.dev/

# Provider API keys, if this convention ever moves into the repo.
# The canonical location is ~/.switchboard/, outside any repository.
.switchboard/

.DS_Store

# Private working notes — names companies and pricing, not for publication
POSITIONING.md
BUSINESS.md
